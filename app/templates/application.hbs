<div class="container">
<div class="row">
  <div class="col-md-5">
    <nav class="navbar navbar-default">
      <div class="container-fluid flex">
        <ul class="nav navbar-nav">
          {{#if playlist}}
            <li>{{#link-to 'playlist' playlist.id}}Playlist{{/link-to}}</li>
          {{else}}
            <li>{{#link-to 'index'}}Playlist{{/link-to}}</li>
          {{/if}}
          <li>{{#link-to 'search'}}Search{{/link-to}}</li>
        </ul>
      </div>
    </nav>

    {{outlet}}
  </div>

  <div class="player col-md-7 flex-center">
    <div id="player"></div>
  </div>
</div>
</div>
